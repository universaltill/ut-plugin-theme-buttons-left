# Buttons Left Theme

Layout theme for Universal Till POS: the product buttons move to the **left**
side of the main page and the sale screen (basket) sits on the **right**, with
journal and tender stacked next to it. Asset-only plugin (`runtime: "none"`);
registers one `type: "theme"` entry. After install, pick it under
Settings → Theme.
